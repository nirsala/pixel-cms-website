node_modules/
*.log
.env

# SEO daily task secrets (Resend key) — never commit
.seo-secrets.env

# Local Cloudflare Wrangler cache/state
.wrangler/

# נתוני ריצה של האפליקציה — מכילים PII, טוקנים ו-hash של סיסמת אדמין.
# האתר מוגש סטטית מה-repo, ולכן כל קובץ כאן נחשף לאינטרנט.
data/

node_modules/
*.log
.env

# SEO daily task secrets (Resend key) — never commit
.seo-secrets.env

# Local Cloudflare Wrangler cache/state
.wrangler/


# data/ נשאר במעקב אך מכיל אך ורק תבניות ריקות.
# ההוסט מגיש את ה-repo סטטית ואינו מוחק קבצים שהוסרו ממנו, ולכן מחיקה
# אינה סוגרת חשיפה — רק דריסה בתוכן ריק כן. הנתונים האמיתיים נמצאים
# מחוץ ל-repo: C:\Users\nirsa\dds-data-private
